const APIConstant = {
  CONTENT_TYPE: 'application/json',
}

const PathPram = {
    PATH_PARAM_USER_ID: '{user_id}'
}

const Request = {
  GET_USERS: 'users/',
}

export {
  APIConstant,
  PathPram,
  Request,
}