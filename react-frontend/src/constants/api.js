const APIConstant = {
  CONTENT_TYPE: 'application/json',
}

const PathPram = {
  PATH_PARAM_USER_ID: '{user_id}'
}

const Request = {
  GET_USERS: 'users/',
  GET_MEDIA: 'api/media'
}

export {
  APIConstant,
  PathPram,
  Request,
}